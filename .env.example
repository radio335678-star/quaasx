# generate a random secret: https://generate-secret.vercel.app/32 or `openssl rand -base64 32`
AUTH_SECRET=****

# AI² Rust Env (Modal ai2-rust-env) — chat via POST /exec → agent_run.py
# No OpenRouter key on Vercel (key lives in Modal secret openrouter-key)
# Live:  https://quaasx--ai2-rust-env-rustenvgateway-web.modal.run
AI2_BACKEND_URL=https://quaasx--ai2-rust-env-rustenvgateway-web.modal.run

# Web-X search engine — Modal ai2-web-x (separate from classical chat)
# Production: https://quaasx--ai2-web-x-webxgateway-web.modal.run
# On Vercel, if unset, proxy auto-uses the Modal URL above. Local dev: http://127.0.0.1:5000
WEBX_BACKEND_URL=https://quaasx--ai2-web-x-webxgateway-web.modal.run
# Required on Vercel for /api/webx/search and /api/webx/config (same as Modal secret webx-keys)
WEBX_API_SECRET=

# Optional — Postgres / Blob / AI Gateway (not required for public AI² chat)
AI_GATEWAY_API_KEY=
BLOB_READ_WRITE_TOKEN=
POSTGRES_URL=
REDIS_URL=
