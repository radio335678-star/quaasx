# generate a random secret: https://generate-secret.vercel.app/32 or `openssl rand -base64 32`
AUTH_SECRET=****

# AI² Hybrid Engine backend (local or Modal) — NO OpenRouter key on Vercel
# Local:  http://127.0.0.1:8090
# Modal:  https://quaasx--ai2-hybrid-engine-web.modal.run
AI2_BACKEND_URL=http://127.0.0.1:8090

# Optional — Postgres / Blob / AI Gateway (not required for public AI² chat)
AI_GATEWAY_API_KEY=
BLOB_READ_WRITE_TOKEN=
POSTGRES_URL=
REDIS_URL=
